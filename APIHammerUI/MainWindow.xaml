<Window x:Class="APIHammerUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:APIHammerUI"
        xmlns:converters="clr-namespace:APIHammerUI.Converters"
        mc:Ignorable="d"
        Title="API Hammer - API Testing Tool" Height="700" Width="1400"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E">
    
    <Window.Resources>
        <converters:InverseBoolConverter x:Key="InverseBoolConverter"/>
        <converters:ConnectionStatusConverter x:Key="ConnectionStatusConverter"/>
        <converters:RequestTypeIconConverter x:Key="RequestTypeIconConverter"/>
        <converters:RequestTypeColorConverter x:Key="RequestTypeColorConverter"/>
        <BooleanToVisibilityConverter x:Key="bool2VisibilityConverter" />

        <!-- Dark Theme Color Resources -->
        <SolidColorBrush x:Key="DarkBackground" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="DarkPanel" Color="#2D2D2D"/>
        <SolidColorBrush x:Key="DarkCard" Color="#3C3C3C"/>
        <SolidColorBrush x:Key="DarkBorder" Color="#404040"/>
        <SolidColorBrush x:Key="AccentBlue" Color="#007ACC"/>
        <SolidColorBrush x:Key="AccentBlueHover" Color="#1F85CC"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#CCCCCC"/>
        <SolidColorBrush x:Key="TextMuted" Color="#999999"/>
        <SolidColorBrush x:Key="SuccessGreen" Color="#4CAF50"/>
        <SolidColorBrush x:Key="WarningOrange" Color="#FF9800"/>
        <SolidColorBrush x:Key="ErrorRed" Color="#F44336"/>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBlue}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource AccentBlueHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#0F5A99"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background" Value="{StaticResource DarkCard}"/>
            <Setter Property="BorderBrush" Value="{StaticResource DarkBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4A4A4A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Close Button Style -->
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource ErrorRed}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern TabItem Style -->
        <Style TargetType="TabItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="Margin" Value="0,0,2,0"/>
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Grid>
                            <Grid.ContextMenu>
                                <ContextMenu Background="{StaticResource DarkCard}" 
                                           BorderBrush="{StaticResource DarkBorder}">
                                    <MenuItem Header="Rename" 
                                            Click="RenameTabMenuItem_Click" 
                                            Tag="{Binding}"
                                            Background="{StaticResource DarkCard}"
                                            Foreground="{StaticResource TextPrimary}"/>
                                    <MenuItem Header="Close" 
                                            Click="CloseTabMenuItem_Click" 
                                            Tag="{Binding}"
                                            Background="{StaticResource DarkCard}"
                                            Foreground="{StaticResource TextPrimary}"/>
                                </ContextMenu>
                            </Grid.ContextMenu>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Column="0" 
                                       Text="{Binding Name}" 
                                       VerticalAlignment="Center"
                                       Margin="0,0,8,0"
                                       FontSize="13"
                                       Foreground="{StaticResource TextPrimary}"/>
                            
                            <Button Grid.Column="1" 
                                    Content="×" 
                                    Style="{StaticResource CloseButtonStyle}"
                                    Click="CloseTab_Click"
                                    Tag="{Binding}"/>
                        </Grid>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="6,6,0,0"
                                Margin="{TemplateBinding Margin}">
                            <ContentPresenter x:Name="ContentSite"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Center"
                                            ContentSource="Header"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource DarkCard}"/>
                                <Setter Property="Panel.ZIndex" Value="100"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource DarkPanel}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Tab Explorer Item Style -->
        <Style x:Key="TabExplorerItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Border" 
                                Padding="12,10" 
                                Background="Transparent"
                                BorderThickness="0,0,0,1"
                                BorderBrush="{StaticResource DarkBorder}">
                            <Border.ContextMenu>
                                <ContextMenu Background="{StaticResource DarkCard}" 
                                           BorderBrush="{StaticResource DarkBorder}">
                                    <MenuItem Header="Rename" 
                                            Click="RenameTabMenuItem_Click" 
                                            Tag="{Binding}"
                                            Background="{StaticResource DarkCard}"
                                            Foreground="{StaticResource TextPrimary}"/>
                                    <MenuItem Header="Close" 
                                            Click="CloseTabMenuItem_Click" 
                                            Tag="{Binding}"
                                            Background="{StaticResource DarkCard}"
                                            Foreground="{StaticResource TextPrimary}"/>
                                </ContextMenu>
                            </Border.ContextMenu>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="24"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Request Type Icon -->
                                <Border Grid.Column="0" 
                                        Width="20" Height="20"
                                        CornerRadius="3"
                                        Background="{Binding RequestType, Converter={StaticResource RequestTypeColorConverter}}"
                                        Margin="0,0,8,0">
                                    <TextBlock Text="{Binding RequestType, Converter={StaticResource RequestTypeIconConverter}}"
                                             HorizontalAlignment="Center"
                                             VerticalAlignment="Center"
                                             FontSize="10"
                                             Foreground="White"
                                             FontWeight="Bold"/>
                                </Border>
                                
                                <!-- Tab Name -->
                                <TextBlock Grid.Column="1" 
                                           Text="{Binding Name}" 
                                           VerticalAlignment="Center"
                                           TextTrimming="CharacterEllipsis"
                                           FontSize="13"
                                           Foreground="{StaticResource TextPrimary}"/>
                                
                                <!-- Close Button -->
                                <Button Grid.Column="2" 
                                        Content="×" 
                                        Style="{StaticResource CloseButtonStyle}"
                                        Click="CloseTab_Click"
                                        Tag="{Binding}"
                                        Visibility="Hidden"
                                        x:Name="CloseButton"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentBlue}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource DarkCard}"/>
                                <Setter TargetName="CloseButton" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="True"/>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="CloseButton" Property="Visibility" Value="Visible"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern TabControl Style -->
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="{StaticResource DarkCard}"/>
            <Setter Property="BorderBrush" Value="{StaticResource DarkBorder}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Tab Strip -->
                            <Border Grid.Row="0" 
                                    Background="{StaticResource DarkPanel}"
                                    BorderBrush="{StaticResource DarkBorder}"
                                    BorderThickness="0,0,0,1">
                                <TabPanel x:Name="HeaderPanel"
                                         IsItemsHost="True"
                                         Margin="12,8,12,0"/>
                            </Border>
                            
                            <!-- Content Area -->
                            <Border Grid.Row="1"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter x:Name="PART_SelectedContentHost"
                                                ContentSource="SelectedContent"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource DarkBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Modern Toolbar -->
        <Border Grid.Row="0" Grid.ColumnSpan="3" 
                Background="{StaticResource DarkPanel}"
                BorderBrush="{StaticResource DarkBorder}"
                BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" 
                        Margin="16,12">
                <Button Content="+ HTTP" 
                        Click="NewHttpTab_Click" 
                        Style="{StaticResource ModernButtonStyle}"
                        Margin="0,0,8,0"/>
                <Button Content="+ WebSocket" 
                        Click="NewWebSocketTab_Click" 
                        Style="{StaticResource SecondaryButtonStyle}"
                        Margin="0,0,8,0"/>
                <Button Content="+ gRPC" 
                        Click="NewGrpcTab_Click" 
                        Style="{StaticResource SecondaryButtonStyle}"
                        Margin="0,0,16,0"/>
                
                <Rectangle Width="1" 
                          Fill="{StaticResource DarkBorder}" 
                          Margin="0,4"/>
                
                <TextBlock Text="Right-click tabs to rename or close" 
                          VerticalAlignment="Center" 
                          Margin="16,0,0,0"
                          FontSize="12"
                          Foreground="{StaticResource TextMuted}"/>
            </StackPanel>
        </Border>

        <!-- Tab Explorer Panel -->
        <Border Grid.Row="1" Grid.Column="0" 
                Background="{StaticResource DarkPanel}"
                BorderBrush="{StaticResource DarkBorder}"
                BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Tab Explorer Header -->
                <Border Grid.Row="0" 
                        Background="{StaticResource DarkCard}"
                        BorderBrush="{StaticResource DarkBorder}"
                        BorderThickness="0,0,0,1"
                        Padding="16,12">
                    <TextBlock Text="ACTIVE TABS" 
                               FontWeight="Bold" 
                               FontSize="11"
                               Foreground="{StaticResource TextMuted}"/>
                </Border>
                
                <!-- Tab List -->
                <ListBox Grid.Row="1" 
                         x:Name="TabExplorer"
                         ItemsSource="{Binding Tabs}"
                         SelectedItem="{Binding SelectedTab}"
                         Background="Transparent"
                         BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ItemContainerStyle="{StaticResource TabExplorerItemStyle}">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                </ListBox>
                
                <!-- Empty State -->
                <StackPanel Grid.Row="1" 
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Visibility="{Binding HasNoTabs, Converter={StaticResource bool2VisibilityConverter}}">
                    <Ellipse Width="48" Height="48" 
                            Fill="{StaticResource DarkCard}"
                            Margin="0,0,0,16"/>
                    <TextBlock Text="No tabs open"
                              HorizontalAlignment="Center"
                              FontSize="14"
                              Foreground="{StaticResource TextMuted}"
                              Margin="0,0,0,8"/>
                    <TextBlock Text="Create a new request to get started"
                              HorizontalAlignment="Center"
                              FontSize="12"
                              Foreground="{StaticResource TextMuted}"
                              TextWrapping="Wrap"
                              TextAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Splitter -->
        <GridSplitter Grid.Row="1" Grid.Column="1" 
                      Width="1"
                      Background="{StaticResource DarkBorder}"
                      HorizontalAlignment="Stretch"
                      ResizeBehavior="PreviousAndNext"/>

        <!-- Main Content Area -->
        <Grid Grid.Row="1" Grid.Column="2" Background="{StaticResource DarkCard}">
            <!-- Tab Control -->
            <TabControl x:Name="MainTabControl"
                        ItemsSource="{Binding Tabs}"
                        SelectedItem="{Binding SelectedTab}"
                        TabStripPlacement="Top">
                <TabControl.ContentTemplate>
                    <DataTemplate>
                        <ContentPresenter Content="{Binding Content}"/>
                    </DataTemplate>
                </TabControl.ContentTemplate>
            </TabControl>

            <!-- Welcome Screen -->
            <Grid Visibility="{Binding HasNoTabs, Converter={StaticResource bool2VisibilityConverter}}"
                  Background="{StaticResource DarkCard}">
                <StackPanel HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           MaxWidth="400">
                    <!-- Logo/Icon -->
                    <Border Width="80" Height="80"
                           Background="{StaticResource AccentBlue}"
                           CornerRadius="40"
                           Margin="0,0,0,24">
                        <TextBlock Text="⚡"
                                  FontSize="36"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  Foreground="White"/>
                    </Border>
                    
                    <TextBlock Text="Welcome to API Hammer"
                              FontSize="28"
                              FontWeight="Light"
                              HorizontalAlignment="Center"
                              Foreground="{StaticResource TextPrimary}"
                              Margin="0,0,0,16"/>
                    
                    <TextBlock Text="A powerful API testing tool for modern developers"
                              FontSize="16"
                              HorizontalAlignment="Center"
                              Foreground="{StaticResource TextSecondary}"
                              TextAlignment="Center"
                              Margin="0,0,0,32"/>
                    
                    <StackPanel Orientation="Horizontal" 
                               HorizontalAlignment="Center">
                        <Button Content="Create HTTP Request"
                               Click="NewHttpTab_Click"
                               Style="{StaticResource ModernButtonStyle}"
                               Margin="0,0,8,0"/>
                        <Button Content="WebSocket"
                               Click="NewWebSocketTab_Click"
                               Style="{StaticResource SecondaryButtonStyle}"
                               Margin="0,0,8,0"/>
                        <Button Content="gRPC"
                               Click="NewGrpcTab_Click"
                               Style="{StaticResource SecondaryButtonStyle}"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>
