<UserControl x:Class="APIHammerUI.Views.WebSocketRequestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Unloaded="UserControl_Unloaded">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- URL and Connect -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" 
                     Text="{Binding Url, UpdateSourceTrigger=PropertyChanged}"
                     VerticalAlignment="Center"
                     Margin="0,0,10,0"
                     Padding="5"/>

            <Button Grid.Column="1" 
                    Content="{Binding ConnectButtonText}" 
                    Click="ConnectButton_Click"
                    VerticalAlignment="Center"/>
        </Grid>

        <!-- Status -->
        <TextBlock Grid.Row="1" 
                   Text="{Binding IsConnected, Converter={StaticResource ConnectionStatusConverter}}"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <!-- Messages Label -->
        <TextBlock Grid.Row="2" Text="Messages:" FontWeight="Bold" Margin="0,0,0,5"/>

        <!-- Messages Display -->
        <TextBox Grid.Row="3" 
                 Text="{Binding Messages}"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 Background="LightGray"
                 Margin="0,0,0,10"/>

        <!-- Send Message -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="80"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" 
                     Text="{Binding Message, UpdateSourceTrigger=PropertyChanged}"
                     VerticalAlignment="Center"
                     Margin="0,0,10,0"
                     Padding="5"
                     IsEnabled="{Binding IsConnected}"/>

            <Button Grid.Column="1" 
                    Content="Send" 
                    Click="SendMessageButton_Click"
                    IsEnabled="{Binding IsConnected}"
                    VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</UserControl>