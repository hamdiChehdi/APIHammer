<UserControl x:Class="APIHammerUI.Views.HttpRequestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#3C3C3C">
    
    <UserControl.Resources>
        <!-- Dark Theme Resources -->
        <SolidColorBrush x:Key="DarkBackground" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="DarkPanel" Color="#2D2D2D"/>
        <SolidColorBrush x:Key="DarkCard" Color="#3C3C3C"/>
        <SolidColorBrush x:Key="DarkBorder" Color="#404040"/>
        <SolidColorBrush x:Key="AccentBlue" Color="#007ACC"/>
        <SolidColorBrush x:Key="AccentBlueHover" Color="#1F85CC"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#CCCCCC"/>
        <SolidColorBrush x:Key="TextMuted" Color="#999999"/>
        <SolidColorBrush x:Key="SuccessGreen" Color="#4CAF50"/>
        <SolidColorBrush x:Key="ErrorRed" Color="#F44336"/>

        <!-- Dark TextBox Style -->
        <Style x:Key="DarkTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource DarkPanel}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource DarkBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"
                                        Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Header TextBox Style (smaller padding) -->
        <Style x:Key="HeaderTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource DarkTextBoxStyle}">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="32"/>
        </Style>

        <!-- Dark ComboBox Style -->
        <Style x:Key="DarkComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource DarkPanel}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource DarkBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <ContentPresenter Grid.Column="0"
                                                    Content="{TemplateBinding SelectionBoxItem}"
                                                    ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                    Margin="{TemplateBinding Padding}"
                                                    VerticalAlignment="Center"
                                                    HorizontalAlignment="Left"/>
                                    
                                    <ToggleButton Grid.Column="0" Grid.ColumnSpan="2"
                                                  x:Name="ToggleButton"
                                                  Background="Transparent"
                                                  BorderThickness="0"
                                                  IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                  ClickMode="Press"/>
                                    
                                    <Path Grid.Column="1"
                                          Data="M 0 0 L 4 4 L 8 0 Z"
                                          Fill="{StaticResource TextMuted}"
                                          Margin="8"
                                          IsHitTestVisible="False"/>
                                </Grid>
                            </Border>
                            
                            <Popup x:Name="PART_Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border Background="{StaticResource DarkCard}"
                                            BorderBrush="{StaticResource DarkBorder}"
                                            BorderThickness="1"
                                            CornerRadius="4">
                                        <ScrollViewer x:Name="DropDownScrollViewer">
                                            <Grid RenderOptions.ClearTypeHint="Enabled">
                                                <Canvas Height="0" Width="0" HorizontalAlignment="Left" VerticalAlignment="Top">
                                                    <Rectangle x:Name="OpaqueRect"
                                                               Height="{Binding ElementName=DropDownBorder, Path=ActualHeight}"
                                                               Width="{Binding ElementName=DropDownBorder, Path=ActualWidth}"
                                                               Fill="{Binding ElementName=DropDownBorder, Path=Background}"/>
                                                </Canvas>
                                                <ItemsPresenter x:Name="ItemsPresenter"
                                                                KeyboardNavigation.DirectionalNavigation="Contained"
                                                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                            </Grid>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Send Button Style -->
        <Style x:Key="SendButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SuccessGreen}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45A049"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#3D8B40"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="{StaticResource TextMuted}"/>
                                <Setter Property="Foreground" Value="#666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Icon Button Style -->
        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource DarkCard}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Delete Button Style -->
        <Style x:Key="DeleteButtonStyle" TargetType="Button" BasedOn="{StaticResource IconButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource ErrorRed}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- CheckBox Style -->
        <Style x:Key="DarkCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Background" Value="{StaticResource DarkPanel}"/>
            <Setter Property="BorderBrush" Value="{StaticResource DarkBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Border x:Name="CheckBoxBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Width="16" Height="16"
                                    CornerRadius="2">
                                <Path x:Name="CheckMark"
                                      Data="M1,4.5 L4,7.5 L11,0.5"
                                      Stroke="{StaticResource AccentBlue}"
                                      StrokeThickness="2"
                                      Visibility="Collapsed"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="{StaticResource AccentBlue}"/>
                                <Setter TargetName="CheckMark" Property="Stroke" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Request Section Header -->
            <TextBlock Grid.Row="0" 
                       Text="HTTP Request" 
                       FontSize="18" 
                       FontWeight="Medium"
                       Foreground="{StaticResource TextPrimary}"
                       Margin="0,0,0,20"/>

            <!-- URL and Method Row -->
            <Grid Grid.Row="1" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ComboBox Grid.Column="0" 
                          ItemsSource="{Binding Methods}" 
                          SelectedItem="{Binding Method}"
                          Style="{StaticResource DarkComboBoxStyle}"
                          Margin="0,0,12,0"/>

                <TextBox Grid.Column="1" 
                         Text="{Binding Url, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource DarkTextBoxStyle}"
                         Margin="0,0,12,0"
                         FontFamily="Consolas"/>

                <Button Grid.Column="2" 
                        Content="Send Request" 
                        Click="SendButton_Click"
                        IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}"
                        Style="{StaticResource SendButtonStyle}"/>
            </Grid>

            <!-- Full URL Display -->
            <StackPanel Grid.Row="2" Margin="0,0,0,20">
                <TextBlock Text="Full URL (with query parameters):" 
                           FontSize="12" 
                           Foreground="{StaticResource TextMuted}"
                           Margin="0,0,0,4"/>
                <Border Background="{StaticResource DarkBackground}"
                        BorderBrush="{StaticResource DarkBorder}"
                        BorderThickness="1"
                        CornerRadius="4"
                        Padding="12">
                    <TextBlock Text="{Binding FullUrl}"
                               FontFamily="Consolas"
                               FontSize="12"
                               Foreground="{StaticResource TextSecondary}"
                               TextWrapping="Wrap"/>
                </Border>
            </StackPanel>

            <!-- Request Configuration Tabs -->
            <TabControl Grid.Row="3" 
                        Background="{StaticResource DarkCard}"
                        BorderBrush="{StaticResource DarkBorder}"
                        BorderThickness="0"
                        Margin="0,0,0,24">
                <TabControl.Resources>
                    <!-- Tab Item Style -->
                    <Style TargetType="TabItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="16,12"/>
                        <Setter Property="Margin" Value="0,0,2,0"/>
                        <Setter Property="FontSize" Value="13"/>
                        <Setter Property="FontWeight" Value="Medium"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border x:Name="Border"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            Padding="{TemplateBinding Padding}"
                                            CornerRadius="6,6,0,0"
                                            Margin="{TemplateBinding Margin}">
                                        <ContentPresenter x:Name="ContentSite"
                                                        VerticalAlignment="Center"
                                                        HorizontalAlignment="Center"
                                                        ContentSource="Header"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="{StaticResource DarkCard}"/>
                                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                            <Setter Property="Panel.ZIndex" Value="100"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="{StaticResource DarkPanel}"/>
                                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Resources>

                <!-- Query Parameters Tab -->
                <TabItem Header="Query Parameters">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock Grid.Column="0"
                                           Text="Configure URL query parameters" 
                                           FontSize="12" 
                                           Foreground="{StaticResource TextMuted}"/>
                                
                                <Button Grid.Column="1"
                                        Content="+ Add Parameter"
                                        Click="AddQueryParameter_Click"
                                        Style="{StaticResource IconButtonStyle}"
                                        Foreground="{StaticResource AccentBlue}"
                                        Padding="8,4"
                                        FontSize="11"/>
                            </Grid>

                            <!-- Query Parameters List -->
                            <Border Background="{StaticResource DarkPanel}"
                                    BorderBrush="{StaticResource DarkBorder}"
                                    BorderThickness="1"
                                    CornerRadius="4"
                                    Padding="8">
                                <StackPanel>
                                    <!-- Parameter Headers -->
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" Text="" Width="24"/>
                                        <TextBlock Grid.Column="1" 
                                                   Text="Parameter" 
                                                   Foreground="{StaticResource TextMuted}"
                                                   FontSize="11"
                                                   FontWeight="Medium"
                                                   Margin="8,0"/>
                                        <TextBlock Grid.Column="2" 
                                                   Text="Value" 
                                                   Foreground="{StaticResource TextMuted}"
                                                   FontSize="11"
                                                   FontWeight="Medium"
                                                   Margin="8,0"/>
                                        <TextBlock Grid.Column="3" Text="" Width="24"/>
                                    </Grid>

                                    <!-- Query Parameters Items -->
                                    <ItemsControl ItemsSource="{Binding QueryParameters}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,4">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <!-- Enable/Disable Checkbox -->
                                                    <CheckBox Grid.Column="0"
                                                              IsChecked="{Binding IsEnabled}"
                                                              Style="{StaticResource DarkCheckBoxStyle}"
                                                              VerticalAlignment="Center"
                                                              Margin="0,0,8,0"/>

                                                    <!-- Key TextBox -->
                                                    <TextBox Grid.Column="1"
                                                             Text="{Binding Key, UpdateSourceTrigger=PropertyChanged}"
                                                             Style="{StaticResource HeaderTextBoxStyle}"
                                                             Margin="0,0,8,0"
                                                             Tag="Parameter"
                                                             GotFocus="QueryParameter_GotFocus">
                                                        <TextBox.InputBindings>
                                                            <KeyBinding Key="Tab" Command="{x:Static ApplicationCommands.NotACommand}"/>
                                                        </TextBox.InputBindings>
                                                    </TextBox>

                                                    <!-- Value TextBox -->
                                                    <TextBox Grid.Column="2"
                                                             Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                                             Style="{StaticResource HeaderTextBoxStyle}"
                                                             Margin="0,0,8,0"
                                                             Tag="Value"
                                                             GotFocus="QueryParameter_GotFocus">
                                                        <TextBox.InputBindings>
                                                            <KeyBinding Key="Tab" Command="{x:Static ApplicationCommands.NotACommand}"/>
                                                        </TextBox.InputBindings>
                                                    </TextBox>

                                                    <!-- Delete Button -->
                                                    <Button Grid.Column="3"
                                                            Content="X"
                                                            Style="{StaticResource DeleteButtonStyle}"
                                                            Click="DeleteQueryParameter_Click"
                                                            Tag="{Binding}"/>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <!-- Authentication Tab -->
                <TabItem Header="Authentication">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
                        <StackPanel>
                            <TextBlock Text="Configure request authentication" 
                                       FontSize="12" 
                                       Foreground="{StaticResource TextMuted}"
                                       Margin="0,0,0,16"/>

                            <Border Background="{StaticResource DarkPanel}"
                                    BorderBrush="{StaticResource DarkBorder}"
                                    BorderThickness="1"
                                    CornerRadius="4"
                                    Padding="16">
                                <StackPanel>
                                    <!-- Authentication Type -->
                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <TextBlock Grid.Column="0" 
                                                   Text="Type:" 
                                                   VerticalAlignment="Center"
                                                   Foreground="{StaticResource TextSecondary}"/>
                                        
                                        <ComboBox Grid.Column="1"
                                                  ItemsSource="{Binding AuthenticationTypes}"
                                                  SelectedItem="{Binding Authentication.Type}"
                                                  Style="{StaticResource DarkComboBoxStyle}"
                                                  DisplayMemberPath="."/>
                                    </Grid>

                                    <!-- Basic Authentication -->
                                    <StackPanel Visibility="{Binding Authentication.Type, Converter={StaticResource AuthTypeToVisibilityConverter}, ConverterParameter=BasicAuth}">
                                        <Grid Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="120"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <TextBlock Grid.Column="0" 
                                                       Text="Username:" 
                                                       VerticalAlignment="Center"
                                                       Foreground="{StaticResource TextSecondary}"/>
                                            
                                            <TextBox Grid.Column="1"
                                                     Text="{Binding Authentication.Username, UpdateSourceTrigger=PropertyChanged}"
                                                     Style="{StaticResource HeaderTextBoxStyle}"/>
                                        </Grid>
                                        
                                        <Grid Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="120"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <TextBlock Grid.Column="0" 
                                                       Text="Password:" 
                                                       VerticalAlignment="Center"
                                                       Foreground="{StaticResource TextSecondary}"/>
                                            
                                            <PasswordBox Grid.Column="1"
                                                         x:Name="PasswordBox"
                                                         PasswordChanged="PasswordBox_PasswordChanged"
                                                         Background="{StaticResource DarkPanel}"
                                                         Foreground="{StaticResource TextPrimary}"
                                                         BorderBrush="{StaticResource DarkBorder}"
                                                         BorderThickness="1"
                                                         Padding="8"
                                                         Height="32"
                                                         FontSize="12"/>
                                        </Grid>
                                    </StackPanel>

                                    <!-- Bearer Token -->
                                    <StackPanel Visibility="{Binding Authentication.Type, Converter={StaticResource AuthTypeToVisibilityConverter}, ConverterParameter=BearerToken}">
                                        <Grid Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="120"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <TextBlock Grid.Column="0" 
                                                       Text="Token:" 
                                                       VerticalAlignment="Center"
                                                       Foreground="{StaticResource TextSecondary}"/>
                                            
                                            <TextBox Grid.Column="1"
                                                     Text="{Binding Authentication.Token, UpdateSourceTrigger=PropertyChanged}"
                                                     Style="{StaticResource HeaderTextBoxStyle}"
                                                     FontFamily="Consolas"/>
                                        </Grid>
                                    </StackPanel>

                                    <!-- API Key -->
                                    <StackPanel Visibility="{Binding Authentication.Type, Converter={StaticResource AuthTypeToVisibilityConverter}, ConverterParameter=ApiKey}">
                                        <Grid Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="120"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <TextBlock Grid.Column="0" 
                                                       Text="Header Name:" 
                                                       VerticalAlignment="Center"
                                                       Foreground="{StaticResource TextSecondary}"/>
                                            
                                            <TextBox Grid.Column="1"
                                                     Text="{Binding Authentication.ApiKeyHeader, UpdateSourceTrigger=PropertyChanged}"
                                                     Style="{StaticResource HeaderTextBoxStyle}"/>
                                        </Grid>
                                        
                                        <Grid Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="120"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <TextBlock Grid.Column="0" 
                                                       Text="API Key:" 
                                                       VerticalAlignment="Center"
                                                       Foreground="{StaticResource TextSecondary}"/>
                                            
                                            <TextBox Grid.Column="1"
                                                     Text="{Binding Authentication.ApiKeyValue, UpdateSourceTrigger=PropertyChanged}"
                                                     Style="{StaticResource HeaderTextBoxStyle}"
                                                     FontFamily="Consolas"/>
                                        </Grid>
                                    </StackPanel>

                                    <!-- No Authentication Message -->
                                    <TextBlock Visibility="{Binding Authentication.Type, Converter={StaticResource AuthTypeToVisibilityConverter}, ConverterParameter=None}"
                                               Text="No authentication required for this request."
                                               Foreground="{StaticResource TextMuted}"
                                               FontStyle="Italic"
                                               Margin="0,8"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <!-- Headers Tab -->
                <TabItem Header="Headers">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock Grid.Column="0"
                                           Text="Configure HTTP request headers" 
                                           FontSize="12" 
                                           Foreground="{StaticResource TextMuted}"/>
                                
                                <Button Grid.Column="1"
                                        Content="+ Add Header"
                                        Click="AddHeader_Click"
                                        Style="{StaticResource IconButtonStyle}"
                                        Foreground="{StaticResource AccentBlue}"
                                        Padding="8,4"
                                        FontSize="11"/>
                            </Grid>

                            <!-- Headers List -->
                            <Border Background="{StaticResource DarkPanel}"
                                    BorderBrush="{StaticResource DarkBorder}"
                                    BorderThickness="1"
                                    CornerRadius="4"
                                    Padding="8">
                                <StackPanel>
                                    <!-- Header Row -->
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" Text="" Width="24"/>
                                        <TextBlock Grid.Column="1" 
                                                   Text="Key" 
                                                   Foreground="{StaticResource TextMuted}"
                                                   FontSize="11"
                                                   FontWeight="Medium"
                                                   Margin="8,0"/>
                                        <TextBlock Grid.Column="2" 
                                                   Text="Value" 
                                                   Foreground="{StaticResource TextMuted}"
                                                   FontSize="11"
                                                   FontWeight="Medium"
                                                   Margin="8,0"/>
                                        <TextBlock Grid.Column="3" Text="" Width="24"/>
                                    </Grid>

                                    <!-- Headers Items -->
                                    <ItemsControl ItemsSource="{Binding Headers}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,4">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <!-- Enable/Disable Checkbox -->
                                                    <CheckBox Grid.Column="0"
                                                              IsChecked="{Binding IsEnabled}"
                                                              Style="{StaticResource DarkCheckBoxStyle}"
                                                              VerticalAlignment="Center"
                                                              Margin="0,0,8,0"/>

                                                    <!-- Key TextBox -->
                                                    <TextBox Grid.Column="1"
                                                             Text="{Binding Key, UpdateSourceTrigger=PropertyChanged}"
                                                             Style="{StaticResource HeaderTextBoxStyle}"
                                                             Margin="0,0,8,0"
                                                             Tag="Key"
                                                             GotFocus="TextBox_GotFocus">
                                                        <TextBox.InputBindings>
                                                            <KeyBinding Key="Tab" Command="{x:Static ApplicationCommands.NotACommand}"/>
                                                        </TextBox.InputBindings>
                                                    </TextBox>

                                                    <!-- Value TextBox -->
                                                    <TextBox Grid.Column="2"
                                                             Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                                             Style="{StaticResource HeaderTextBoxStyle}"
                                                             Margin="0,0,8,0"
                                                             Tag="Value"
                                                             GotFocus="TextBox_GotFocus">
                                                        <TextBox.InputBindings>
                                                            <KeyBinding Key="Tab" Command="{x:Static ApplicationCommands.NotACommand}"/>
                                                        </TextBox.InputBindings>
                                                    </TextBox>

                                                    <!-- Delete Button -->
                                                    <Button Grid.Column="3"
                                                            Content="X"
                                                            Style="{StaticResource DeleteButtonStyle}"
                                                            Click="DeleteHeader_Click"
                                                            Tag="{Binding}"/>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <!-- Body Tab -->
                <TabItem Header="Body">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
                        <StackPanel>
                            <TextBlock Text="Configure request body content" 
                                       FontSize="12" 
                                       Foreground="{StaticResource TextMuted}"
                                       Margin="0,0,0,12"/>
                            
                            <TextBox Text="{Binding Body, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource DarkTextBoxStyle}"
                                     Height="300"
                                     TextWrapping="Wrap"
                                     AcceptsReturn="True"
                                     VerticalScrollBarVisibility="Auto"
                                     FontFamily="Consolas"
                                     FontSize="12"/>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>
            </TabControl>

            <!-- Response Section Header -->
            <TextBlock Grid.Row="4" 
                       Text="Response" 
                       FontSize="14" 
                       FontWeight="Medium" 
                       Foreground="{StaticResource TextPrimary}"
                       Margin="0,0,0,8"/>

            <!-- Response Section -->
            <Border Grid.Row="5"
                    Background="{StaticResource DarkBackground}"
                    BorderBrush="{StaticResource DarkBorder}"
                    BorderThickness="1"
                    CornerRadius="4"
                    MinHeight="200">
                <TextBox Text="{Binding Response}"
                         IsReadOnly="True"
                         Background="Transparent"
                         Foreground="{StaticResource TextSecondary}"
                         BorderThickness="0"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         Padding="12"
                         FontFamily="Consolas"
                         FontSize="12"/>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>