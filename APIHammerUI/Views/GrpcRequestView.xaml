<UserControl x:Class="APIHammerUI.Views.GrpcRequestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Server URL -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Server:" VerticalAlignment="Center" FontWeight="Bold"/>
            <TextBox Grid.Column="1" 
                     Text="{Binding Server, UpdateSourceTrigger=PropertyChanged}"
                     VerticalAlignment="Center"
                     Padding="5"/>
        </Grid>

        <!-- Service -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Service:" VerticalAlignment="Center" FontWeight="Bold"/>
            <TextBox Grid.Column="1" 
                     Text="{Binding Service, UpdateSourceTrigger=PropertyChanged}"
                     VerticalAlignment="Center"
                     Padding="5"/>
        </Grid>

        <!-- Method -->
        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Method:" VerticalAlignment="Center" FontWeight="Bold"/>
            <TextBox Grid.Column="1" 
                     Text="{Binding Method, UpdateSourceTrigger=PropertyChanged}"
                     VerticalAlignment="Center"
                     Margin="0,0,10,0"
                     Padding="5"/>

            <Button Grid.Column="2" 
                    Content="Call" 
                    Click="CallButton_Click"
                    IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}"
                    VerticalAlignment="Center"/>
        </Grid>

        <!-- Request Label -->
        <TextBlock Grid.Row="3" Text="Request (JSON):" FontWeight="Bold" Margin="0,0,0,5"/>

        <!-- Request Body -->
        <TextBox Grid.Row="4" 
                 Text="{Binding Request, UpdateSourceTrigger=PropertyChanged}"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 Margin="0,0,0,10"/>

        <!-- Response Label -->
        <TextBlock Grid.Row="5" Text="Response:" FontWeight="Bold" Margin="0,0,0,5"/>

        <!-- Response -->
        <TextBox Grid.Row="6" 
                 Text="{Binding Response}"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 Background="LightGray"/>
    </Grid>
</UserControl>